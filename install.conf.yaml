- clean: ['~']


- link:
    # ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.tmux: tmux/
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc 
- shell:
    - [git submodule update --init --recursive, Installing submodules]
