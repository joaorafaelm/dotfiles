- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.config/fish: fish/

- shell:
    - [git submodule update --init --recursive, Installing submodules]
